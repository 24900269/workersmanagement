services:
  # ─── BACKEND WEB SERVICE ─────────────────────────────────────
  - type: web
    name: trackx-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DB_PATH
        value: /var/data/trackx.db
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false # Set this to your frontend URL in the dashboard to enable CORS

  # ─── FRONTEND STATIC SITE ─────────────────────────────────────
  - type: web
    name: trackx-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        sync: false # Set this to your backend Render URL (e.g. https://trackx-api.onrender.com/api) to connect the frontend to the backend
    routes:
      # Rewrite all requests to index.html for client-side React routing (fixes 404s on page refresh or deep links)
      - type: rewrite
        source: /*
        destination: /index.html
