# TrackX Frontend - Environment Variables Template

# Base URL of the backend Express API (Render production link or local fallback)
# Note: For Vite to load variables, they must be prefixed with "VITE_"
VITE_API_URL=https://workersmanagement-backend.onrender.com/api
